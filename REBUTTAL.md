# REBUTTAL — 预期 Reviewer 质疑与应对方案

> 本文档列出论文 `report/report.tex` 在投稿/答辩中可能面临的主要 reviewer 攻击点，以及针对每个问题的可执行 rebuttal 策略。
>
> 每个条目包含：攻击描述、严重程度、当前论文中已有的防御、不足、可执行改进。
>
> **已更新为移除 B-D 后的六模型纯 Holling II 框架。**

---

## 攻击 1：模型复杂度增加但真实数据拟合提升不稳定

### 攻击描述

fear-memory 比 baseline 多 1 个动力学参数 + 1 个 ODE（记忆状态），但留后验证仅在少数序列上单独最优。AICc 标准下 baseline 赢了 13/15。扩展到六模型后，不同恐惧通道在不同序列获胜，没有一个通道一致胜出。额外复杂度没有一致的预测回报——这恰好是 null hypothesis（恐惧效应不存在）的期望结果。

### 严重程度：★★★（最高）

这是 reviewer 最有可能提出的核心质疑。

### 论文中已有的防御

- 六模型比较表格展示了跨序列的赢家多样性
- 结论明确区分了"预测层面"和"机制层面"
- AICc 已经按实际拟合参数数量进行了惩罚

### 不足

- 没有报告 ΔAICc 的实质性阈值（>2, >4, >7），仅统计"最优"
- 没有报告恐惧模型胜出时的改善幅度（许多 margin 可能很小）
- 没有对 RMSE 改善做不确定性估计（bootstrap CI）
- 没有分析恐惧参数在 AICc-baseline-win 序列中是否被推至接近零
- GLERL 序列上 baseline、fear-memory 及多个恐惧通道的 RMSE 完全相等——这是一个关键诊断点但未被突出

### 可执行改进

1. **报告 ΔAICc 分布**：对每条序列计算 baseline 与最佳恐惧模型的 ΔAICc；报告 ΔAICc > 2（实质支持）、> 4（强支持）的序列各有多少。如果大多数序列 ΔAICc < 2，则胜负对微小数值波动敏感。

2. **退化检验**：绘制 AICc(baseline) - AICc(fear-memory) vs fitted φ 的散点图。预期正相关——baseline 越占优的序列，φ 越接近 0。如果成立，则"AICc 支持 baseline"等价于"数据中恐惧信号不足"。

3. **突出 GLERL 诊断案例**：GLERL 上多模型 RMSE 完全相等，说明额外结构在该序列上不做任何功。应在正文中单独讨论。

### 对应 TODO

- TODO #30（退化检验）、#31（ΔAICc 阈值）

---

## 攻击 2：Holling II 系列恐惧模型没有稳定优于 baseline

### 攻击描述

在 AICc 标准下 baseline 赢了 13/15，六模型扩展中不同恐惧通道在不同序列胜出。这些结果模式与"恐惧效应不存在"的 null hypothesis 一致——如果恐惧没有一致的生态效应，那么表现最好的模型会在各序列间随机游走，而最简单的模型会在 AICc 上占优。

### 严重程度：★★★（最高）

### 论文中已有的防御

- 六模型热力图清楚展示了跨序列的赢家多样性
- 正文明确写了"该扩展仍不支持'某一种恐惧通道普遍正确'"
- 结论确认了这一点

### 不足

- 没有对赢家分布做形式化的随机性检验
- 没有计算 pooled Akaike weights 来量化 baseline 的总体证据权重
- "不同通道在不同序列获胜"被表述为一个中性发现，但 reviewer 可能将其解释为反对恐惧效应的证据

### 可执行改进

1. **Pooled Akaike weights**：将 15 个序列视为独立似然（尽管有研究内重复的限定），计算每个模型的 pooled Akaike weight。如果 baseline 的 pooled weight 远高（如 > 0.7），则基线在总体证据上的优势是明确的。

2. **按研究聚合**：如果 6 个独立研究/系统级别上（Andrén 全七区聚合、Killifish 三站聚合、Windermere 两湖盆聚合），恐惧模型的胜出更加不稳定，则进一步支持 null hypothesis。

3. **正面承认此模式**：在讨论中增加以下表述："在 AICc 框架下，最简单的 baseline 模型获得了总体证据支持；恐惧模型的验证胜出集中在部分序列，且胜出通道因序列而异。这种模式与'恐惧机制存在但信号弱、数据不足以一致区分通道'的解释兼容。"

### 对应 TODO

- TODO #33（研究级聚合）

---

## 攻击 3：模拟实验和真实数据拟合之间的联系不够强

### 攻击描述

理论部分（第 6.2 节）用默认参数正平衡点处 20% 等效抑制校准了 5 种恐惧机制。结论是"不同机制走不同方向"——但这从方程结构就可以定性推断（繁殖抑制降低 r → 猎物增长减小 → 捕食者减少；觅食抑制降低 a → 捕食率降低 → 猎物增加捕食者增加），不一定需要大规模数值实验来证明。

另一方面，真实数据拟合中，fear-memory 的 φ 常触边或接近 0。理论实验的 20% 设定与数据中接近 0 的估计之间存在未讨论的 gap。

### 严重程度：★★☆（中高）

### 论文中已有的防御

- 五种机制的长期均值变化表量化了方向差异
- 正文区分了"理论性质"和"数据推断"

### 不足

- 没有讨论"如果数据中恐惧抑制接近 0，为什么理论实验中要用 20%"
- 20% 的选取依据没有在正文中交代

### 可执行改进

1. **20% 选取依据**：在附录 B 中增加一句说明——"20% 是文献中常用于校准比较的中间值（参考 Zanette 2003 等），反映的是中等偏悲观的恐惧情景，旨在排除'不同机制仅在强度极弱时才结论一致'的可能。"

2. **在讨论中明确桥接 gap**："理论实验中的 20% 是 a priori 基准值。真实数据中 φ 普遍接近 0 的估计表明，当前数据集合中的恐惧信号不足以达到该基准强度。因此机制路径依赖性是理论层面的稳健结论，但真实恐惧强度在此类数据中难以估计。"

### 需要确认的问题

- "理论 20% 值的选取需要额外依据"——当前论文没有提供选取理由。如果 reviewer 质疑，你打算引用文献依据还是承认这是一个任意的基准？

---

## 攻击 4：参数可解释性、可识别性或统计显著性不足

### 攻击描述

(a) 拟合参数没有标准误或置信区间；
(b) 多数拟合触边（参数被推至搜索边界），尤其是 e、K、φ 等；
(c) M(0) profile 显示 14/15 条序列的初始记忆在 [0, 5y(0)] 内不可辨识，δ 训练最优 ≠ 验证最优；
(d) 没有参数之间的协方差/相关性分析——当 e 和 μ 高度负相关时，单个点估计在生态上不可解读。

### 严重程度：★★☆（中高）

### 论文中已有的防御

- 明确标注了每条拟合的触边参数
- 讨论中承认了参数补偿的存在
- M(0) 和 δ profile 结果支持固定 M(0)=y(0)、δ=1

### 不足

- 全文没有任何参数标准误、CI 或不确定性量化
- 触边参数只在表注中出现，没有在正文中整合讨论
- 参数之间的相关性从未被分析
- 没有区分"可用于预测的参数"和"可单独解释的参数"

### 可执行改进

1. **参数不确定性量化**：至少对 baseline 做。优先使用 Fisher 信息矩阵（利用 L-BFGS-B 的 Hessian 近似），如果不可用则使用 parametric bootstrap（10 次以内）。

2. **参数相关性**：报告 e 和 μ 在 15 条序列上的相关系数。如果高度负相关（r < -0.8），讨论中指出"这两个参数在 Holling II 框架内结构性地难以分离识别"。

3. **触边参数讨论**：在讨论中增加一段触边参数的汇总和解释——触边不意味拟合失败（模型仍可用于预测），但点估计可信度不足，不应被解读为真实生态速率。

4. **区分两种参数用途**：在方法中增加一句——"(i) 预测性参数：在给定模型内使轨迹误差最小化，不要求独立可解释；(ii) 结构性参数：需在 profile 或 bootstrap 下保持合理精度才可用于生态推断。"

### 对应 TODO

- TODO #32（参数不确定性量化）

### 需要确认的问题

- 是否有 Hessian 近似或 bootstrap 的工具链就绪？如果 Fisher 信息矩阵不可用，是否接受 10 次 bootstrap？

---

## 攻击 5：图表过多但没有集中支持核心结论

### 攻击描述

正文约 15 页，含 15+ 张图和 8+ 张表。论文的两个核心结论（机制路径依赖性、预测≠机制识别）实际上只需要 3-4 个核心 exhibit。理论分析图表和实验图表混排，读者可能迷失而无法抓住主论点。

### 严重程度：★☆☆（中等）

### 论文中已有的防御

- 图表均有明确的标题和标签
- 论文结构有 section/subsection 层级

### 不足

- 理论与实验的图表混排在一起
- 多个图表（如时序图、φ 扫描、δ 扫描等）支持同一级别论点但以独立 Figure 存在
- 没有"路径图"指引读者核心图表的位置

### 可执行改进

正文保留 4 个核心图表，其余移至附录：
1. 六模型留后验证热力图
2. 六模型胜负汇总表
3. M(0) 和 δ profile（新 6.5 节）
4. 一个代表性拟合图

### 对应 TODO

- TODO #34（图表精简）

---

## 攻击 6：15 条序列不是 15 个独立样本（伪重复）

### 攻击描述

Andrén 七个区域来自同一研究（同种配对、共享研究设计），Killifish 三站同理，Windermere 两湖盆同理。至多约 6 个独立研究。论文的所有胜负统计按 15 个拟合单位计算，夸大了证据量。

### 严重程度：★★★（最高）

### 论文中已有的防御

- 附录 A 及正文 L306 明确标注了 Andrén 七区共享研究设计

### 不足

- 正文中所有胜负统计数字均未按研究级调整
- 没有研究级聚合分析作为主结果的敏感性分析

### 可执行改进

1. **序列级 + 研究级双报告**：Andrén 七区取组内验证 RMSE 中位数；Killifish 三站同理；Windermere 两湖盆同理。重新计算 N≈6 的研究级胜负表。建议同时报告中位数、最佳、最差三种聚合方式以展示稳健性。

2. **正文首次出现"15 条序列"时加脚注**说明非独立性。

### 对应 TODO

- TODO #26（序列计数限定）、#33（研究级聚合）

### 需要确认的问题

- Andrén 七区是取中位数、最差（最保守）、还是最佳（最乐观）作为聚合指标？建议同时报告三者——若三者一致，结论稳健；若不一致，这本身是需要讨论的结果。

---

## 攻击 7：论文没有验证任何恐惧机制在真实数据中存在

### 攻击描述

论文的核心叙事是"恐惧效应有机制路径依赖性"+"真实数据中预测改善≠机制识别"+"恐惧参数不可辨识"。综合起来，论文实际上没有为"恐惧效应在真实系统中存在"提供任何正面证据。reviewer 可能问：如果恐惧既不能被识别、也不能被估计、也不能被证实，这篇论文对生态学的贡献是什么？

### 严重程度：★★☆（中高）

### 论文中已有的防御

- 论文结论明确停留在"预测能力"层面而非"机制证明"层面
- 没有做出过度宣称

### 不足

- 论文没有明确陈述自己的正面贡献（negative result 也是贡献，但需要被正面框定）
- 讨论中没有解释"这种 negative result 对领域意味着什么"

### 可执行改进

在讨论中增加一个正面框定段落：

> "本研究的贡献不在于'发现恐惧效应'或'确证某种恐惧机制'，而在于：(1) 系统展示了五个恐惧通道在相同抑制强度下的定性分歧——这意味着在缺乏独立机制证据时，任何单一通道的拟合成功都不应被解释为'我们找到了正确的恐惧机制'；(2) 在对 15 个公开时间序列的统一比较中，没有一个恐惧通道稳定优于不含恐惧的 baseline——这是一个有信息量的 negative result，因为它约束了'在此类宏观种群数据中能被检测到的恐惧效应量'的上限；(3) 参数 profile 分析表明，即使恐惧模型在预测上有所改善，恐惧参数本身也无法被可靠估计——因此文献中基于此类数据拟合的恐惧强度报告需要重新审视。"

---

## 附录 A：语言问题速查表

以下句子需要在 P5 阶段修改，按严重程度排列（已移除 B-D 相关的行）：

| 行号 | 问题类型 | 原文摘要 | 建议修改方向 |
|------|---------|---------|------------|
| L189 | 过度宣称 | "31/31 共存" | "31/31 在积分范围内未灭绝" |
| L202 | 口语化 | "没有脱离机制路径的统一答案" | "种群变化的定性方向取决于恐惧的机制路径" |
| L183 | 辩护语气 | "但图形本身不能替代长期诊断" | "瞬态图形需配合收敛诊断判断长期行为" |
| L306-307 | 草稿语气 | "一是...二是...三是...四是..." | 改为正式段落，三条核心原因即可 |
| L250 | 未限定 | "45 次拟合均通过" | 增加"其中 X 次 success, Y 次 usable_limit" |
| L454 | 辩护语气 | "默认参数机制实验仅作为补充" | 正面表述："本节仅展示默认参数下的理论性质" |
| L43 | 摘要中的序列计数 | "15 条真实种群序列" | 加脚注说明非独立性；或用"15 个拟合单位" |
| L76-78 | 需删除 | "跨系统直接比较裸 k 没有明确生态含义" | B-D 相关，Phase 1 中删除 |
| L57 | 需更新 | "三模型预测比较" | 改为六模型框架 |
